//
// PlaylistSearchParams.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct PlaylistSearchParams: Sendable, Codable, Hashable {

    public enum Sort: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case date = "date"
        case artist = "artist"
        case song = "song"
        case dj = "dj"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    public enum Order: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case asc = "asc"
        case desc = "desc"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    public static let pageRule = NumericRule<Int>(minimum: 0, exclusiveMinimum: false, maximum: nil, exclusiveMaximum: false, multipleOf: nil)
    public static let limitRule = NumericRule<Int>(minimum: 1, exclusiveMinimum: false, maximum: 100, exclusiveMaximum: false, multipleOf: nil)
    /** Search query (supports AND, OR, NOT, \"\", *) */
    public var q: String?
    public var page: Int? = 0
    public var limit: Int? = 50
    public var sort: Sort? = .date
    public var order: Order? = .desc

    public init(q: String? = nil, page: Int? = 0, limit: Int? = 50, sort: Sort? = .date, order: Order? = .desc) {
        self.q = q
        self.page = page
        self.limit = limit
        self.sort = sort
        self.order = order
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case q
        case page
        case limit
        case sort
        case order
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(q, forKey: .q)
        try container.encodeIfPresent(page, forKey: .page)
        try container.encodeIfPresent(limit, forKey: .limit)
        try container.encodeIfPresent(sort, forKey: .sort)
        try container.encodeIfPresent(order, forKey: .order)
    }
}


extension PlaylistSearchParams: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if sort == .unknownDefaultOpenApi { return true }
        if order == .unknownDefaultOpenApi { return true }
        return false
    }
}
