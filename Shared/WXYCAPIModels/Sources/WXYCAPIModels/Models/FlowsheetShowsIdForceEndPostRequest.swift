//
// FlowsheetShowsIdForceEndPostRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct FlowsheetShowsIdForceEndPostRequest: Sendable, Codable, Hashable {

    /** Override the derived end instant. Omit it and the show is closed at its last logged entry (floored at `start_time`), which is the right answer for the abandoned shows this endpoint exists to reach and is what the description above documents.  Supply it when an operator knows better than the flowsheet does — a show whose DJ stopped logging an hour before they actually went off the air, say. Bounded on both ends, or the request is a 400 naming the bound it missed.  The upper bound is now: a show may not claim to have ended in the future. The lower bound is the derived instant this field overrides — the show's last logged entry, floored at `start_time` — so an override may only ever move the close LATER than the flowsheet's own answer. A show with nothing logged has no last entry, and there the floor is `start_time`.  Closing a show BEFORE entries it still owns is therefore not on offer, and it is the mirror image of the `now()` hazard rather than a different one. The supplied instant is also the `show_end` marker's `add_time`, and the public flowsheet sorts globally by `add_time` — so where `now()` floats the sign-off above every real entry, an instant behind the show's own entries sinks it below them, and the flowsheet renders a show that signs off and then keeps playing.  `GET /flowsheet/range` is affected less than it might seem: it admits any show an in-window entry references, so a truncated show is not lost from a window that contains its entries and their `show_id` stays resolvable. What it does lose is a window containing none of them but lying inside the show's real airtime — the range read then reports that the show did not air.  When the entries are the thing that is wrong — a later DJ's rows absorbed into an abandoned show — they are re-pointed to the show that aired them, not truncated around.  */
    public var endedAt: Date?

    public init(endedAt: Date? = nil) {
        self.endedAt = endedAt
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case endedAt = "ended_at"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(endedAt, forKey: .endedAt)
    }
}

