//
// ArtistSearchMatch.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ArtistSearchMatch: Sendable, Codable, Hashable {

    public var id: Int
    public var artistName: String
    public var codeLetters: String
    /** The artist's genre-scoped code number (`genre_artist_crossreference.artist_genre_code`) — one row per (artist, genre) membership, so a multi-genre artist appears once per genre with different numbers.  */
    public var codeNumber: Int
    /** The genre whose membership this row represents. Declared because the library-wide search (the mode with no `genre_id` parameter) returns memberships from mixed genres, where the row alone no longer says which genre its `code_number` belongs to. Present on every row Backend returns, in both search modes: one query builder serves both, its `genre_artist_crossreference` and `genres` joins are INNER either way, and `genre_artist_crossreference.genre_id` is NOT NULL with an FK. Optional here all the same — no consumer requires the guarantee, and narrowing a published response field breaks any client generated against the loose shape. Read \"optional\" as \"the contract declines to promise it\", not \"the server omits it\".  */
    public var genreId: Int?
    /** The genre's display name, from the same INNER join as `genre_id` (`genres.genre_name` is NOT NULL). Same treatment: returned on every row, optional in the contract for the same reason.  */
    public var genreName: String?

    public init(id: Int, artistName: String, codeLetters: String, codeNumber: Int, genreId: Int? = nil, genreName: String? = nil) {
        self.id = id
        self.artistName = artistName
        self.codeLetters = codeLetters
        self.codeNumber = codeNumber
        self.genreId = genreId
        self.genreName = genreName
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case artistName = "artist_name"
        case codeLetters = "code_letters"
        case codeNumber = "code_number"
        case genreId = "genre_id"
        case genreName = "genre_name"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(artistName, forKey: .artistName)
        try container.encode(codeLetters, forKey: .codeLetters)
        try container.encode(codeNumber, forKey: .codeNumber)
        try container.encodeIfPresent(genreId, forKey: .genreId)
        try container.encodeIfPresent(genreName, forKey: .genreName)
    }
}

