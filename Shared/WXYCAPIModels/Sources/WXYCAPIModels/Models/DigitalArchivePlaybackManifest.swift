//
// DigitalArchivePlaybackManifest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Response body for GET /digital-archive/albums/{id}/playback (#417) — the auto-DJ archive player&#39;s client contract (epic WXYC/wxyc-dj-ios#135). Deliberately outlives every storage decision: no bucket, key, or store name appears anywhere in this shape, only opaque presigned URLs that die at &#x60;expires_at&#x60;.  &#x60;provenance&#x60; is per-track, not per-manifest: the digital_asset unique key is (library_id, provenance, disc_number), so one album can hold several bound assets — a multi-disc set today, and a cd_rip beside an existing rotation_upload once CD digitization starts. A manifest merges them, so a single manifest-level value would have had to pick a winner and misreport every track sourced from the others.  */
public struct DigitalArchivePlaybackManifest: Sendable, Codable, Hashable {

    /** The album's wxyc_schema.library.id — the same key CatalogExportRow.id ships. */
    public var libraryId: Int
    /** Every URL under `tracks` is dead after this instant — refetch this manifest rather than caching URLs past it.  */
    public var expiresAt: Date
    public var tracks: [DigitalArchivePlaybackTrack]

    public init(libraryId: Int, expiresAt: Date, tracks: [DigitalArchivePlaybackTrack]) {
        self.libraryId = libraryId
        self.expiresAt = expiresAt
        self.tracks = tracks
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case libraryId = "library_id"
        case expiresAt = "expires_at"
        case tracks
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(libraryId, forKey: .libraryId)
        try container.encode(expiresAt, forKey: .expiresAt)
        try container.encode(tracks, forKey: .tracks)
    }
}

