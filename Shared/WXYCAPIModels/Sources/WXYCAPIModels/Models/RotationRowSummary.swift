//
// RotationRowSummary.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** One &#x60;rotation&#x60; row projected to its published ten-column surface — the shape &#x60;GET /library/rotation/uncatalogued&#x60;, &#x60;GET /library/rotation/{id}&#x60; and &#x60;PATCH /library/rotation/{rotation_id}/link&#x60; all share (Backend&#39;s &#x60;toRotationRowSummary&#x60;, BS#2109/BS#2410). Referent rule: &#x60;format_id&#x60; and &#x60;label_id&#x60; here are the **rotation row&#39;s own pre-catalog fields** — captured at rotation-add, written only while &#x60;album_id&#x60; is NULL, retained after linking. They never describe the linked library release; that is the opposite of &#x60;GET /library/rotation&#x60;, whose joined rows read library-side fields where a link exists.  */
public struct RotationRowSummary: Sendable, Codable, Hashable {

    /** The rotation row's own id — the id Kill/Unkill/link act on. */
    public var id: Int
    /** Linked `library.id`, or null while uncatalogued. Rows from `/library/rotation/uncatalogued` are null by construction.  */
    public var albumId: Int?
    public var rotationBin: RotationBin
    /** Always present — `rotation.add_date` is NOT NULL with a `defaultNow()`, and it is the queue's sort key (newest first).  */
    public var addDate: CalendarDate
    /** May be future-dated: a scheduled kill leaves the row active until the date passes.  */
    public var killDate: CalendarDate?
    public var artistName: String?
    public var albumTitle: String?
    /** Free-text label snapshot. Retained even when `label_id` is set, for rows whose text never resolved to a `labels` row.  */
    public var recordLabel: String?
    /** Pre-catalog `format(id)` reference (BS#2409). Null on every row created before the dj-site classic add form began sending it; the legacy backfill is WXYC/Backend-Service#2412.  */
    public var formatId: Int?
    /** Pre-catalog `labels(id)` reference (BS#2409). Same null population and backfill story as `format_id`.  */
    public var labelId: Int?

    public init(id: Int, albumId: Int? = nil, rotationBin: RotationBin, addDate: CalendarDate, killDate: CalendarDate? = nil, artistName: String? = nil, albumTitle: String? = nil, recordLabel: String? = nil, formatId: Int? = nil, labelId: Int? = nil) {
        self.id = id
        self.albumId = albumId
        self.rotationBin = rotationBin
        self.addDate = addDate
        self.killDate = killDate
        self.artistName = artistName
        self.albumTitle = albumTitle
        self.recordLabel = recordLabel
        self.formatId = formatId
        self.labelId = labelId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case albumId = "album_id"
        case rotationBin = "rotation_bin"
        case addDate = "add_date"
        case killDate = "kill_date"
        case artistName = "artist_name"
        case albumTitle = "album_title"
        case recordLabel = "record_label"
        case formatId = "format_id"
        case labelId = "label_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(albumId, forKey: .albumId)
        try container.encode(rotationBin, forKey: .rotationBin)
        try container.encode(addDate, forKey: .addDate)
        try container.encodeIfPresent(killDate, forKey: .killDate)
        try container.encodeIfPresent(artistName, forKey: .artistName)
        try container.encodeIfPresent(albumTitle, forKey: .albumTitle)
        try container.encodeIfPresent(recordLabel, forKey: .recordLabel)
        try container.encodeIfPresent(formatId, forKey: .formatId)
        try container.encodeIfPresent(labelId, forKey: .labelId)
    }
}


extension RotationRowSummary: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if rotationBin == .unknownDefaultOpenApi { return true }
        return false
    }
}
