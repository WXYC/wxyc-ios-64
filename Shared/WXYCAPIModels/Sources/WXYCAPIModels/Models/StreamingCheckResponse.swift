//
// StreamingCheckResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct StreamingCheckResponse: Sendable, Codable, Hashable {

    /** True if found on any service, false if all services confirmed absent (no errors), null if inconclusive — either no services checked OR at least one service raised an error (see `errored_sources`). Treat null as \"do not persist\" / \"retry later\".  */
    public var onStreaming: Bool?
    public var sources: StreamingCheckSources
    /** Names of services whose check raised an exception (transient network/rate-limit/ scraping failure). Empty when every dispatched check completed without raising. When non-empty, callers should consider the listed services unchecked and may schedule a selective retry. Independent of `on_streaming`: a service can both match (populating `sources.<service>`) and other services can error.  */
    public var erroredSources: [String]?

    public init(onStreaming: Bool?, sources: StreamingCheckSources, erroredSources: [String]? = nil) {
        self.onStreaming = onStreaming
        self.sources = sources
        self.erroredSources = erroredSources
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case onStreaming = "on_streaming"
        case sources
        case erroredSources = "errored_sources"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(onStreaming, forKey: .onStreaming)
        try container.encode(sources, forKey: .sources)
        try container.encodeIfPresent(erroredSources, forKey: .erroredSources)
    }
}

