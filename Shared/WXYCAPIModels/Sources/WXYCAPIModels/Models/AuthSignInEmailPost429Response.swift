//
// AuthSignInEmailPost429Response.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** One of two shapes depending on which of the two layers above answered. Modeled as a oneOf rather than a single schema so a generated client&#39;s type reflects both real possibilities rather than silently assuming the better-auth shape always wins — verified this generates cleanly across all four codegen targets: openapi-typescript emits a proper union type (the one generator that surfaces a per-status response body at all); the Python (datamodel-code-generator), Swift (openapi-generator-cli swift6), and Kotlin generators don&#39;t model inline path-response bodies by status code in the first place, so both member schemas simply continue to generate as independent, unaffected models. NOTE: the better-auth-layer member (AuthRateLimitedResponse) is not reliably decodable as JSON in the first place regardless of this modeling — see that schema&#39;s own description for the Content-Type caveat; a &#x60;oneOf&#x60; communicates the shape a caller gets IF it parses the body as JSON, not a guarantee that Content-Type says so.  */
public enum AuthSignInEmailPost429Response: Sendable, Codable, Hashable {
    case typeAuthPlainErrorResponse(AuthPlainErrorResponse)
    case typeAuthRateLimitedResponse(AuthRateLimitedResponse)
    case unknownDefaultOpenApi

    public func encode(to encoder: Encoder) throws {
        var container = encoder.singleValueContainer()
        switch self {
        case .typeAuthPlainErrorResponse(let value):
            try container.encode(value)
        case .typeAuthRateLimitedResponse(let value):
            try container.encode(value)
        case .unknownDefaultOpenApi:
            try container.encodeNil()
        }
    }

    public init(from decoder: Decoder) throws {
        let container = try decoder.singleValueContainer()
        if let value = try? container.decode(AuthPlainErrorResponse.self), (value as? UnknownCaseCheckable)?.containsUnknownDefaultOpenApiCase != true {
            self = .typeAuthPlainErrorResponse(value)
        } else if let value = try? container.decode(AuthRateLimitedResponse.self), (value as? UnknownCaseCheckable)?.containsUnknownDefaultOpenApiCase != true {
            self = .typeAuthRateLimitedResponse(value)
        } else {
            self = .unknownDefaultOpenApi
        }
    }
}

