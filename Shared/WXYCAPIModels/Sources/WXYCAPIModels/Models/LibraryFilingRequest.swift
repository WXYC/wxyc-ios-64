//
// LibraryFilingRequest.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Composite of the three sequential writes filing a new record into rotation previously required (create artist, create release, create rotation entry) — see &#x60;POST /library/filings&#x60; (#454). All-or-nothing: a mid-chain failure rolls back the whole request rather than stranding partial state for the client to reconcile.  */
public struct LibraryFilingRequest: Sendable, Codable, Hashable {

    public var artist: FilingArtist
    public var release: AlbumCreateFields
    public var rotation: FilingRotationRequest?

    public init(artist: FilingArtist, release: AlbumCreateFields, rotation: FilingRotationRequest? = nil) {
        self.artist = artist
        self.release = release
        self.rotation = rotation
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case artist
        case release
        case rotation
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(artist, forKey: .artist)
        try container.encode(release, forKey: .release)
        try container.encodeIfPresent(rotation, forKey: .rotation)
    }
}

