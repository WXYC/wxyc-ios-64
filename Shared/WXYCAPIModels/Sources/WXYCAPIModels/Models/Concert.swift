//
// Concert.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** One upcoming (or recent) concert, as served by &#x60;GET /concerts&#x60;.  Date semantics: &#x60;starts_on&#x60; is the venue-local (America/New_York) calendar date and is always present — clients should window and sort on it. &#x60;starts_at&#x60; is the exact start instant and is null for date-only events (many sources publish no start time; WXYC never fabricates one).  Internal ingestion columns (&#x60;source&#x60;, &#x60;source_id&#x60;, &#x60;raw_data&#x60;, &#x60;scraped_at&#x60;, &#x60;first_scraped_at&#x60;) are deliberately not exposed.  */
public struct Concert: Sendable, Codable, Hashable {

    public var id: Int
    public var venue: Venue
    /** Venue-local (America/New_York) calendar date. */
    public var startsOn: Date
    /** Exact start instant; null for date-only events. */
    public var startsAt: Date?
    /** Doors-open instant, when the source publishes one. */
    public var doorsAt: Date?
    /** Headliner billing string exactly as the source displays it. */
    public var headliningArtistRaw: String
    /** WXYC catalog artist id when the resolver matched the headliner; null otherwise. A non-null id is what `curated=true` filters on. */
    public var headliningArtistId: Int?
    /** Event name as the source displays it, when distinct from the artist billing. */
    public var title: String?
    /** Supporting-act billing strings, in source order. */
    public var supportingArtistsRaw: [String]
    public var ticketUrl: String?
    public var imageUrl: String?
    /** The venue's own event-detail page, distinct from `ticket_url` (often a third-party seller like Etix/Ticketmaster). Null when no venue page is known; clients fall back to `ticket_url`. */
    public var eventUrl: String?
    /** Dollars. Free events carry price_min = 0. */
    public var priceMin: Double?
    /** Dollars. */
    public var priceMax: Double?
    /** Source-displayed age restriction (e.g. \"18+\", \"All Ages\"). */
    public var ageRestriction: String?
    public var status: ConcertStatus
    /** Discogs genre tags for the resolved headlining artist, aggregated across their releases (LML discogs-cache, majority-take). Null when the headliner is unresolved or enrichment has not run. Optional (not in `required`) so the field can land ahead of the Backend-Service emitter and older clients decode forward-compatibly — same discipline as `FlowsheetV2TrackEntry.upcoming_show`. Same taxonomy as `FlowsheetV2TrackEntry.genres`. */
    public var genres: [String]?
    /** Top-K affinity neighbors of the resolved headliner, computed nightly from the semantic-index graph and ordered by `weight` descending. Null when the headliner is unresolved or enrichment has not run. Powers on-device For You matching (set intersection against liked-artist ids). Optional (not in `required`) so it can land ahead of the Backend-Service emitter and older clients decode forward-compatibly — same discipline as `Concert.genres`. */
    public var similarArtists: [SimilarArtist]?
    /** Deprecated — use `station_recommended` instead. Play counts were rejected as the station-tier signal in the \"WXYC recommends\" redesign (WXYC/wxyc-ios-64#576); the tier now keys on rotation membership. All-time WXYC flowsheet play count of the resolved (in-library) headliner, computed nightly from the semantic-index graph; null when the headliner is not in the library or has no play count. Identical for every listener. Stays on the wire until the removal ticket (WXYC/Backend-Service#1732) clears its adoption gate; will be removed in a future major version. */
    @available(*, deprecated, message: "This property is deprecated.")
    public var stationPlays: Int?
    /** True when the concert's resolved headliner (`headlining_artist_id`) has at least one WXYC library release that has been in rotation — any rotation row, past or present (\"has been in rotation\", not \"currently in rotation\"). Omitted or false for concerts with no library-resolved headliner, including Discogs-only resolutions (`headlining_discogs_artist_id` without `headlining_artist_id` in the Backend), which by construction have no library releases to hold rotation membership. The rotation-membership signal behind the On Tour \"For You\" station tier (\"WXYC recommends\", WXYC/wxyc-ios-64#576); replaces the deprecated `station_plays`. This is the GATE (rotation membership) — see `station_recommended_rank` for the plays-ordered selection rank within that gate. Identical for every listener; carries no listener data. Optional (not in `required`) so it can land ahead of the Backend-Service emitter and older clients decode forward-compatibly — same discipline as `Concert.similar_artists`. */
    public var stationRecommended: Bool?
    /** 1-based rank of this concert within the station's \"WXYC recommends\" set — among upcoming concerts whose resolved headliner is in rotation (station_recommended = true), ordered by the station's ranking signal (all-time WXYC plays), rank 1 = strongest. Null when the concert is not in the set (headliner not rotation-gated). This is the field the client sorts the station tier by; station_recommended is the gate (rotation membership), this is the plays-ordered selection rank within that gate. Identical for every listener; carries no listener data. Optional (not in `required`) and nullable so it can land ahead of the Backend-Service emitter and older clients decode forward-compatibly — same discipline as `Concert.station_plays`. */
    public var stationRecommendedRank: Int?
    /** Artist biography from the resolved headliner's Discogs profile (raw Discogs markup, parsed client-side), keyed on the effective Discogs artist id and cached nightly on `artist_metadata`. Null when the headliner is unresolved, has no Discogs profile, or enrichment has not run. Identical for every listener; carries no listener data. Renders the On Tour concert-detail \"About the Artist\" card. Optional (not in `required`) so it can land ahead of the Backend-Service emitter and older clients decode forward-compatibly — same discipline as `Concert.similar_artists`. */
    public var artistBio: String?

    public init(id: Int, venue: Venue, startsOn: Date, startsAt: Date?, doorsAt: Date?, headliningArtistRaw: String, headliningArtistId: Int?, title: String?, supportingArtistsRaw: [String], ticketUrl: String?, imageUrl: String?, eventUrl: String?, priceMin: Double?, priceMax: Double?, ageRestriction: String?, status: ConcertStatus, genres: [String]? = nil, similarArtists: [SimilarArtist]? = nil, stationPlays: Int? = nil, stationRecommended: Bool? = nil, stationRecommendedRank: Int? = nil, artistBio: String? = nil) {
        self.id = id
        self.venue = venue
        self.startsOn = startsOn
        self.startsAt = startsAt
        self.doorsAt = doorsAt
        self.headliningArtistRaw = headliningArtistRaw
        self.headliningArtistId = headliningArtistId
        self.title = title
        self.supportingArtistsRaw = supportingArtistsRaw
        self.ticketUrl = ticketUrl
        self.imageUrl = imageUrl
        self.eventUrl = eventUrl
        self.priceMin = priceMin
        self.priceMax = priceMax
        self.ageRestriction = ageRestriction
        self.status = status
        self.genres = genres
        self.similarArtists = similarArtists
        self.stationPlays = stationPlays
        self.stationRecommended = stationRecommended
        self.stationRecommendedRank = stationRecommendedRank
        self.artistBio = artistBio
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case venue
        case startsOn = "starts_on"
        case startsAt = "starts_at"
        case doorsAt = "doors_at"
        case headliningArtistRaw = "headlining_artist_raw"
        case headliningArtistId = "headlining_artist_id"
        case title
        case supportingArtistsRaw = "supporting_artists_raw"
        case ticketUrl = "ticket_url"
        case imageUrl = "image_url"
        case eventUrl = "event_url"
        case priceMin = "price_min"
        case priceMax = "price_max"
        case ageRestriction = "age_restriction"
        case status
        case genres
        case similarArtists = "similar_artists"
        case stationPlays = "station_plays"
        case stationRecommended = "station_recommended"
        case stationRecommendedRank = "station_recommended_rank"
        case artistBio = "artist_bio"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(venue, forKey: .venue)
        try container.encode(startsOn, forKey: .startsOn)
        try container.encode(startsAt, forKey: .startsAt)
        try container.encode(doorsAt, forKey: .doorsAt)
        try container.encode(headliningArtistRaw, forKey: .headliningArtistRaw)
        try container.encode(headliningArtistId, forKey: .headliningArtistId)
        try container.encode(title, forKey: .title)
        try container.encode(supportingArtistsRaw, forKey: .supportingArtistsRaw)
        try container.encode(ticketUrl, forKey: .ticketUrl)
        try container.encode(imageUrl, forKey: .imageUrl)
        try container.encode(eventUrl, forKey: .eventUrl)
        try container.encode(priceMin, forKey: .priceMin)
        try container.encode(priceMax, forKey: .priceMax)
        try container.encode(ageRestriction, forKey: .ageRestriction)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(genres, forKey: .genres)
        try container.encodeIfPresent(similarArtists, forKey: .similarArtists)
        try container.encodeIfPresent(stationPlays, forKey: .stationPlays)
        try container.encodeIfPresent(stationRecommended, forKey: .stationRecommended)
        try container.encodeIfPresent(stationRecommendedRank, forKey: .stationRecommendedRank)
        try container.encodeIfPresent(artistBio, forKey: .artistBio)
    }
}


extension Concert: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if status == .unknownDefaultOpenApi { return true }
        return false
    }
}
