//
// LibraryFilingConflictError.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** The &#x60;POST /library/filings&#x60; 409 body. Purpose-built rather than a &#x60;$ref&#x60; to the shared &#x60;ApiErrorResponse&#x60;, following &#x60;ShowAlreadyOpenError&#x60;: on the artist-conflict reasons a client is expected to ACT on the conflicting &#x60;artist&#x60; row (offer \&quot;use the existing artist instead\&quot; and resubmit with &#x60;kind: existing&#x60;), not merely display a message — and the deployed artist-create 409 shape &#x60;{message, reason, artist}&#x60; does not match &#x60;ApiErrorResponse&#x60; anyway.  */
public struct LibraryFilingConflictError: Sendable, Codable, Hashable {

    public var message: String
    public var reason: LibraryFilingConflictReason
    /** The already-catalogued artist that collided. Present on `artist_code_conflict` (the holder of the taken `(code_letters, genre_id, code_number)` triple) and `artist_name_conflict` (the genre-scoped fold-matched name); absent on `rotation_card_bin_mismatch`, which has no artist to name.  */
    public var artist: Artist?

    public init(message: String, reason: LibraryFilingConflictReason, artist: Artist? = nil) {
        self.message = message
        self.reason = reason
        self.artist = artist
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case message
        case reason
        case artist
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(message, forKey: .message)
        try container.encode(reason, forKey: .reason)
        try container.encodeIfPresent(artist, forKey: .artist)
    }
}


extension LibraryFilingConflictError: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if reason == .unknownDefaultOpenApi { return true }
        return false
    }
}
