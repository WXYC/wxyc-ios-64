//
// ArtistGenresResultItem.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Per-artist aggregation, index-aligned with the request &#x60;artists&#x60; list.  */
public struct ArtistGenresResultItem: Sendable, Codable, Hashable {

    /** Echoed from the request for index alignment. */
    public var artistName: String
    /** Echoed from the request (explicit null when not supplied). Kept out of `required` deliberately: datamodel-codegen (LML's generator) types a required-plus-nullable field as non-nullable and would reject the null this field must carry — the same treatment as `ArtistResolveResult.discogs_artist_id`.  */
    public var discogsArtistId: Int?
    /** Top coarse Discogs genres (majority-take, frequency-ranked, truncated to the top-K). Empty when unknown. The ~15-value Discogs genre taxonomy the iOS On Tour filter chips render.  */
    public var genres: [String]
    /** Frequency-ranked Discogs styles (full list, untruncated). Empty when unknown. The finer Discogs style taxonomy; the iOS filter ignores it — it is a detail-view treatment.  */
    public var styles: [String]
    public var source: ArtistGenresSource

    public init(artistName: String, discogsArtistId: Int? = nil, genres: [String], styles: [String], source: ArtistGenresSource) {
        self.artistName = artistName
        self.discogsArtistId = discogsArtistId
        self.genres = genres
        self.styles = styles
        self.source = source
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case artistName = "artist_name"
        case discogsArtistId = "discogs_artist_id"
        case genres
        case styles
        case source
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(artistName, forKey: .artistName)
        try container.encodeIfPresent(discogsArtistId, forKey: .discogsArtistId)
        try container.encode(genres, forKey: .genres)
        try container.encode(styles, forKey: .styles)
        try container.encode(source, forKey: .source)
    }
}


extension ArtistGenresResultItem: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if source == .unknownDefaultOpenApi { return true }
        return false
    }
}
