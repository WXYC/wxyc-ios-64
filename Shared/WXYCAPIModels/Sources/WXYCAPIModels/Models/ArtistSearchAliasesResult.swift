//
// ArtistSearchAliasesResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct ArtistSearchAliasesResult: Sendable, Codable, Hashable {

    /** The WXYC canonical artist name from the request. */
    public var name: String
    public var variants: [ArtistSearchAliasVariant]
    /** Sources the composer actually queried for this artist. A source appears here iff the composer reached its fetch code path — independent of whether that path produced any variants. Consumers use this to scope reconciliation: only delete cached rows whose `source` is listed here. Sources skipped because of missing prerequisites (e.g., no `discogs_artist_id` in `entity.identity`) are ABSENT from this list, and the consumer must leave their cached rows alone — that source did not run, so its absence is not evidence of upstream deletion. Modeled on the empty-provenance semantic of `BulkResolveProvenanceEntry`.  */
    public var sourcesPresent: [ArtistSearchAliasSource]

    public init(name: String, variants: [ArtistSearchAliasVariant], sourcesPresent: [ArtistSearchAliasSource]) {
        self.name = name
        self.variants = variants
        self.sourcesPresent = sourcesPresent
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case name
        case variants
        case sourcesPresent = "sources_present"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(name, forKey: .name)
        try container.encode(variants, forKey: .variants)
        try container.encode(sourcesPresent, forKey: .sourcesPresent)
    }
}

