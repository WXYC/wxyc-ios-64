//
// DigitalArchivePlaybackTrackRenditionsInner.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct DigitalArchivePlaybackTrackRenditionsInner: Sendable, Codable, Hashable {

    public enum Codec: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case mp3 = "mp3"
        case aac = "aac"
        case flac = "flac"
        case m4a = "m4a"
        case wav = "wav"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    /** The five formats the archive actually holds, matching digital_asset_file.codec in Backend-Service's schema. The bulk is mp3; `library/freeform/` also carries FLAC, m4a and wav, and the enum lists them so a bound row can never be unrepresentable here.  */
    public var codec: Codec
    public var bitrateKbps: Int?
    /** Presigned GET. Opaque; a bearer credential until `expires_at` on the enclosing manifest.  */
    public var url: String

    public init(codec: Codec, bitrateKbps: Int? = nil, url: String) {
        self.codec = codec
        self.bitrateKbps = bitrateKbps
        self.url = url
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case codec
        case bitrateKbps = "bitrate_kbps"
        case url
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(codec, forKey: .codec)
        try container.encodeIfPresent(bitrateKbps, forKey: .bitrateKbps)
        try container.encode(url, forKey: .url)
    }
}


extension DigitalArchivePlaybackTrackRenditionsInner: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if codec == .unknownDefaultOpenApi { return true }
        return false
    }
}
