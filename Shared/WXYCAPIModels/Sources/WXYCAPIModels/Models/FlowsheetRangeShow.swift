//
// FlowsheetRangeShow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Show metadata for one show overlapping a &#x60;GET /flowsheet/range&#x60; window. A deliberately public-safe projection of the &#x60;shows&#x60; row: it carries the DJ&#39;s public handle but no user id and no real-name column. Distinct from &#x60;Show&#x60;, which exposes &#x60;primary_dj_id&#x60; and is used on authenticated surfaces. */
public struct FlowsheetRangeShow: Sendable, Codable, Hashable {

    /** Matches `FlowsheetRangeEntry.show_id` for every entry belonging to this show. */
    public var id: Int
    /** Specialty-show name, or `null` for a regular show. */
    public var showName: String?
    /** Resolved public display name of the show's DJ. Same PII-safe resolution chain as `FlowsheetEntryFields.dj_name` (BS#1371): per-show override -> `user.djName` -> `shows.legacy_dj_name` (tubafrenzy's `DJ_HANDLE`) -> null. Never the real-name column. */
    public var djName: String?
    public var specialtyId: Int?
    /** When the DJ signed on (ISO 8601). */
    public var startTime: Date
    /** When the DJ signed off (ISO 8601), or `null`. Null has **two** causes and they are not distinguishable from this field alone: the show is genuinely on the air, or its `show_end` delivery was dropped and the column stayed null permanently (nothing re-closes it on a schedule). Do not render a null `end_time` as \"on air now\"; compare `start_time` against the present, or read the `show_end` marker row in `entries`, which is the second independent signal. */
    public var endTime: Date?

    public init(id: Int, showName: String? = nil, djName: String? = nil, specialtyId: Int? = nil, startTime: Date, endTime: Date? = nil) {
        self.id = id
        self.showName = showName
        self.djName = djName
        self.specialtyId = specialtyId
        self.startTime = startTime
        self.endTime = endTime
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case showName = "show_name"
        case djName = "dj_name"
        case specialtyId = "specialty_id"
        case startTime = "start_time"
        case endTime = "end_time"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encodeIfPresent(showName, forKey: .showName)
        try container.encodeIfPresent(djName, forKey: .djName)
        try container.encodeIfPresent(specialtyId, forKey: .specialtyId)
        try container.encode(startTime, forKey: .startTime)
        try container.encodeIfPresent(endTime, forKey: .endTime)
    }
}

