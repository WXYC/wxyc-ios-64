//
// AutoDJAck.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

public struct AutoDJAck: Sendable, Codable, Hashable {

    public enum ModelType: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case ack = "ack"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    public enum Status: String, Sendable, Codable, CaseIterable, CaseIterableDefaultsLast {
        case ok = "ok"
        case error = "error"
        case unknownCommand = "unknown_command"
        case unknownDefaultOpenApi = "unknown_default_open_api"
    }
    public var type: ModelType
    /** The id from the command being acknowledged */
    public var id: String
    public var status: Status
    /** Error message (only when status is error) */
    public var error: String?
    /** Optional result data (e.g. { active: boolean } for button_toggle acks) */
    public var result: [String: JSONValue]?

    public init(type: ModelType, id: String, status: Status, error: String? = nil, result: [String: JSONValue]? = nil) {
        self.type = type
        self.id = id
        self.status = status
        self.error = error
        self.result = result
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case type
        case id
        case status
        case error
        case result
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(type, forKey: .type)
        try container.encode(id, forKey: .id)
        try container.encode(status, forKey: .status)
        try container.encodeIfPresent(error, forKey: .error)
        try container.encodeIfPresent(result, forKey: .result)
    }
}


extension AutoDJAck: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if type == .unknownDefaultOpenApi { return true }
        if status == .unknownDefaultOpenApi { return true }
        return false
    }
}
