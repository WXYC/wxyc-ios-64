//
// RecentShow.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** One show as reported by &#x60;GET /flowsheet/shows/recent&#x60;. Distinct from &#x60;OpenShow&#x60;: it carries &#x60;end_time&#x60; and the DJ list, and omits the &#x60;entry_count&#x60;, &#x60;is_current&#x60; and &#x60;likely_abandoned&#x60; fields that exist to help an operator decide what to force-end.  Every property is always present on the wire; the nullable ones carry &#x60;null&#x60;, they are not omitted.  */
public struct RecentShow: Sendable, Codable, Hashable {

    /** `shows.id` — the value to pass as `show_id` to `GET /flowsheet/playlist` to read the show itself.  */
    public var id: Int
    public var showName: String?
    public var startTime: Date
    /** `null` while the show is still on the air — and also when its sign-off was lost, which is common enough that `null` must not be read as \"live\". `GET /flowsheet/open-shows` is the read that separates the two, via its `is_current` field.  */
    public var endTime: Date?
    /** Who had the room. `$ref`s the same `OnAirDJ` that `GET /flowsheet/djs-on-air` returns, because Backend composes both from one function (`composeShowDJList`) — a second declaration of the shape is how the two would come to disagree.  Empty when the show carries no account rows AND no resolvable show-level handle. The show is still listed, so a consumer renders \"unknown\" against real times rather than losing the row.  Membership is NOT filtered to still-active DJs, unlike `/flowsheet/djs-on-air`: sign-off deactivates every remaining membership, so filtering would empty the list for every properly ended show. Co-hosts therefore appear alongside the primary DJ.  */
    public var djs: [OnAirDJ]

    public init(id: Int, showName: String?, startTime: Date, endTime: Date?, djs: [OnAirDJ]) {
        self.id = id
        self.showName = showName
        self.startTime = startTime
        self.endTime = endTime
        self.djs = djs
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case showName = "show_name"
        case startTime = "start_time"
        case endTime = "end_time"
        case djs
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(showName, forKey: .showName)
        try container.encode(startTime, forKey: .startTime)
        try container.encode(endTime, forKey: .endTime)
        try container.encode(djs, forKey: .djs)
    }
}

