//
// DiscogsMatchResult.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** A processed Discogs search result with artwork and enriched metadata. Distinct from the raw Discogs API search response, which this contract deliberately does not model -- no WXYC endpoint proxies Discogs unmodified. Streaming URL fields match the StreamingLinks schema and will be migrated to use $ref in a future version. Two distinct states share this shape, told apart by &#x60;release_id&#x60;: a real Discogs identity has &#x60;release_id &gt; 0&#x60; with a non-empty &#x60;release_url&#x60;; the streaming-only sentinel (BS#1185) has &#x60;release_id &#x3D;&#x3D; 0&#x60; and &#x60;release_url &#x3D;&#x3D; \&quot;\&quot;&#x60;, signalling \&quot;I carry only streaming URLs, there is no Discogs release to link or to fetch album metadata from.\&quot; A consumer keying \&quot;has Discogs identity\&quot; on &#x60;release_id !&#x3D; 0&#x60; therefore handles both states correctly, including the row-less identity described on &#x60;LookupResultItem&#x60;.  */
public struct DiscogsMatchResult: Sendable, Codable, Hashable {

    public static let confidenceRule = NumericRule<Double>(minimum: 0, exclusiveMinimum: false, maximum: 1, exclusiveMaximum: false, multipleOf: nil)
    /** Release title */
    public var album: String?
    /** Release artist */
    public var artist: String?
    /** Discogs release ID. `> 0` is a real release identity; `0` is the streaming-only sentinel (paired with `release_url == \"\"`, BS#1185) and is not a linkable Discogs release.  */
    public var releaseId: Int
    /** Discogs master ID for the release, when the release belongs to a master. `null` when Discogs has no master for this release (one-offs, self-released) or for the streaming-only sentinel (`release_id == 0`). Lets a caller collapse multiple pressings/formats of one logical album into a single record keyed on the master. Populated from the resolved release's `master_id` field on `/lookup` (single and bulk).  */
    public var masterId: Int?
    /** URL to the release on Discogs. Non-empty for a real release identity; the empty string accompanies the `release_id == 0` streaming-only sentinel.  */
    public var releaseUrl: String
    /** Artwork image URL */
    public var artworkUrl: String?
    /** Match confidence score */
    public var confidence: Double? = 0
    /** Release year from Discogs */
    public var releaseYear: Int?
    /** Artist biography from Discogs profile */
    public var artistBio: String?
    /** Wikipedia URL for the artist */
    public var wikipediaUrl: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var spotifyUrl: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var appleMusicUrl: String?
    /** Search URL -- a query URL for the release, not a deep link to it. The two search fields are `youtube_music_url` and `soundcloud_url`; neither carries a `StreamingResolution` verdict, because a search URL can always be constructed and so has no probe/absence distinction. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations.  */
    public var youtubeMusicUrl: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var bandcampUrl: String?
    /** Search URL -- a query URL for the release, not a deep link to it. The two search fields are `youtube_music_url` and `soundcloud_url`; neither carries a `StreamingResolution` verdict, because a search URL can always be constructed and so has no probe/absence distinction. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations.  */
    public var soundcloudUrl: String?
    /** Per-service streaming resolution status (verified / absent / unresolved) for this result, disambiguating WHY a sibling `*_url` field is null. A service marked `unresolved` timed out, had its enrichment tail shed, or was a cold cache miss — its null url is transient and MAY resolve on a later retry; `absent` means the service was consulted and genuinely has no match — its null url is terminal and must NOT be re-probed (re-asking `absent` is the per-play LML-call amplifier BS#1747 killed). A service whose key is OMITTED from this object was never consulted (e.g. Bandcamp on the `/lookup/bulk` path, or the library.db override skipped for a non-library row) and must not be treated as `absent`. Additive and optional: null/omitted on responses from an LML predating the producer rollout, or on paths that resolve no per-service status. Does not change the meaning of the `*_url` fields — it only annotates them. Emitted identically on `/lookup` and `/lookup/bulk` (the LML#681 parity rule). Mirrors the per-service verdict vocabulary of `/api/v1/streaming-check` (LML#376). See LML#1053 / BS#1819.  */
    public var streamingStatus: StreamingResolution?
    /** Discogs artist ID for this release. Populated only when the originating `LookupRequest.extended` is true. Lets a caller key an artist-metadata cache without a follow-up release fetch.  */
    public var discogsArtistId: Int?
    /** Release tracklist with per-track artist credits where available. Populated only when `extended` is true.  */
    public var tracklist: [DiscogsTrackItem]?
    /** Discogs genre tags (e.g. \"Rock\", \"Electronic\"). Populated only when `extended` is true.  */
    public var genres: [String]?
    /** Discogs style tags (finer-grained than genres; e.g. \"Indie Rock\", \"Ambient\"). Populated only when `extended` is true.  */
    public var styles: [String]?
    /** Primary record-label name from the Discogs release. Distinct from `LibraryCatalogItem.label` (which comes from the WXYC catalog's rotation-release join). Populated only when `extended` is true.  */
    public var label: String?
    /** Release date as an ISO string (e.g. \"1997-09-22\"). May be year-only (\"1997\") or year-month (\"1997-09\") if Discogs lacks the full date. Populated only when `extended` is true.  */
    public var fullReleaseDate: String?
    /** Primary artist image URL from Discogs (the artist's profile photo, not the release artwork). Populated only when `extended` is true.  */
    public var artistImageUrl: String?
    /** Pre-parsed structured tokens from the artist's `profile` markup, using a cache-only resolver — references to entities not in the local PG cache fall through as plain-text tokens (no inline Discogs API calls on the read path). Populated only when `extended` is true. Field name matches `DiscogsArtistDetails.profile_tokens` so callers can share rendering code across the two payloads. Pair with `LookupRequest.warm_cache=true` on write-path calls to progressively populate the cache so subsequent reads render richer.  */
    public var profileTokens: [DiscogsResolvedToken]?
    public var writerCredits: DiscogsWriterCredits?

    public init(album: String? = nil, artist: String? = nil, releaseId: Int, masterId: Int? = nil, releaseUrl: String, artworkUrl: String? = nil, confidence: Double? = 0, releaseYear: Int? = nil, artistBio: String? = nil, wikipediaUrl: String? = nil, spotifyUrl: String? = nil, appleMusicUrl: String? = nil, youtubeMusicUrl: String? = nil, bandcampUrl: String? = nil, soundcloudUrl: String? = nil, streamingStatus: StreamingResolution? = nil, discogsArtistId: Int? = nil, tracklist: [DiscogsTrackItem]? = nil, genres: [String]? = nil, styles: [String]? = nil, label: String? = nil, fullReleaseDate: String? = nil, artistImageUrl: String? = nil, profileTokens: [DiscogsResolvedToken]? = nil, writerCredits: DiscogsWriterCredits? = nil) {
        self.album = album
        self.artist = artist
        self.releaseId = releaseId
        self.masterId = masterId
        self.releaseUrl = releaseUrl
        self.artworkUrl = artworkUrl
        self.confidence = confidence
        self.releaseYear = releaseYear
        self.artistBio = artistBio
        self.wikipediaUrl = wikipediaUrl
        self.spotifyUrl = spotifyUrl
        self.appleMusicUrl = appleMusicUrl
        self.youtubeMusicUrl = youtubeMusicUrl
        self.bandcampUrl = bandcampUrl
        self.soundcloudUrl = soundcloudUrl
        self.streamingStatus = streamingStatus
        self.discogsArtistId = discogsArtistId
        self.tracklist = tracklist
        self.genres = genres
        self.styles = styles
        self.label = label
        self.fullReleaseDate = fullReleaseDate
        self.artistImageUrl = artistImageUrl
        self.profileTokens = profileTokens
        self.writerCredits = writerCredits
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case album
        case artist
        case releaseId = "release_id"
        case masterId = "master_id"
        case releaseUrl = "release_url"
        case artworkUrl = "artwork_url"
        case confidence
        case releaseYear = "release_year"
        case artistBio = "artist_bio"
        case wikipediaUrl = "wikipedia_url"
        case spotifyUrl = "spotify_url"
        case appleMusicUrl = "apple_music_url"
        case youtubeMusicUrl = "youtube_music_url"
        case bandcampUrl = "bandcamp_url"
        case soundcloudUrl = "soundcloud_url"
        case streamingStatus = "streaming_status"
        case discogsArtistId = "discogs_artist_id"
        case tracklist
        case genres
        case styles
        case label
        case fullReleaseDate = "full_release_date"
        case artistImageUrl = "artist_image_url"
        case profileTokens = "profile_tokens"
        case writerCredits = "writer_credits"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(album, forKey: .album)
        try container.encodeIfPresent(artist, forKey: .artist)
        try container.encode(releaseId, forKey: .releaseId)
        try container.encodeIfPresent(masterId, forKey: .masterId)
        try container.encode(releaseUrl, forKey: .releaseUrl)
        try container.encodeIfPresent(artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(confidence, forKey: .confidence)
        try container.encodeIfPresent(releaseYear, forKey: .releaseYear)
        try container.encodeIfPresent(artistBio, forKey: .artistBio)
        try container.encodeIfPresent(wikipediaUrl, forKey: .wikipediaUrl)
        try container.encodeIfPresent(spotifyUrl, forKey: .spotifyUrl)
        try container.encodeIfPresent(appleMusicUrl, forKey: .appleMusicUrl)
        try container.encodeIfPresent(youtubeMusicUrl, forKey: .youtubeMusicUrl)
        try container.encodeIfPresent(bandcampUrl, forKey: .bandcampUrl)
        try container.encodeIfPresent(soundcloudUrl, forKey: .soundcloudUrl)
        try container.encodeIfPresent(streamingStatus, forKey: .streamingStatus)
        try container.encodeIfPresent(discogsArtistId, forKey: .discogsArtistId)
        try container.encodeIfPresent(tracklist, forKey: .tracklist)
        try container.encodeIfPresent(genres, forKey: .genres)
        try container.encodeIfPresent(styles, forKey: .styles)
        try container.encodeIfPresent(label, forKey: .label)
        try container.encodeIfPresent(fullReleaseDate, forKey: .fullReleaseDate)
        try container.encodeIfPresent(artistImageUrl, forKey: .artistImageUrl)
        try container.encodeIfPresent(profileTokens, forKey: .profileTokens)
        try container.encodeIfPresent(writerCredits, forKey: .writerCredits)
    }
}

