//
// ArtistGenresInput.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** One artist to aggregate genres/styles for. */
public struct ArtistGenresInput: Sendable, Codable, Hashable {

    public static let artistNameRule = StringRule(minLength: 1, maxLength: nil, pattern: nil)
    /** Artist display name. */
    public var artistName: String
    /** Discogs artist ID. Optional but strongly preferred: when present the cache aggregation keys on it (stable Discogs artist entity, homonym-safe); when absent it falls back to a case-insensitive exact `artist_name` match. Backend-Service resolves it via `POST /api/v1/artists/resolve/bulk` (LML#759) before calling this endpoint, so it is usually present.  */
    public var discogsArtistId: Int?

    public init(artistName: String, discogsArtistId: Int? = nil) {
        self.artistName = artistName
        self.discogsArtistId = discogsArtistId
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case artistName = "artist_name"
        case discogsArtistId = "discogs_artist_id"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(artistName, forKey: .artistName)
        try container.encodeIfPresent(discogsArtistId, forKey: .discogsArtistId)
    }
}

