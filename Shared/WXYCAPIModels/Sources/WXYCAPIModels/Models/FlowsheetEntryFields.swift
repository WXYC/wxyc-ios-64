//
// FlowsheetEntryFields.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Every non-identity field of a v1 flowsheet row, factored out so that more than one identity block can carry the identical field set without the copies drifting. &#x60;FlowsheetEntryResponse&#x60; is currently the only composer — the sibling that shared it has since moved to the V2 union — and it stays factored rather than inlined for two reasons: the V1 shape is being retired one caller at a time, and this schema is where the two streaming-URL YAML anchors are defined, which a YAML alias requires to precede every use. Not referenced directly by any path; compose it with an identity block instead. */
public struct FlowsheetEntryFields: Sendable, Codable, Hashable {

    public static let discogsUnavailableNoteRule = StringRule(minLength: nil, maxLength: 500, pattern: nil)
    public var albumId: Int?
    public var trackTitle: String?
    public var albumTitle: String?
    public var artistName: String?
    public var recordLabel: String?
    /** The rotation release's CANONICAL label, resolved server-side `flowsheet.rotation_id -> rotation.label_id -> labels.label_name` (WXYC/Backend-Service#2505).  Distinct from `record_label` above, which is the free text a DJ typed mid-show and is UNCHANGED by this field's addition — it stays the snapshot and the fallback. The two exist together because the weekly airplay report names each ranked line from the FIRST rotation-linked play of the week, and 2.5% of rotation releases carry more than one label spelling across their weekly plays (2.2% differing by more than case), so one variant or one wrong early entry names the whole line. Resolving from the linked release removes the spelling contest at the root: every play of a release reads one row.  Null when the entry has no rotation link, and null when the linked release has no `label_id` — the state of every row until WXYC/Backend-Service#2412 backfills it from tubafrenzy's `COMPANY_ID`, and the permanent state of a release whose label never resolved to a `labels` row. Consumers must fall back to `record_label` on null.  Resolved by rotation id with no status filter, so a KILLED release with plays in the window still names its label — unlike `GET /library/rotation`, which serves active rows only. A historical report must not lose the releases that were retired since the week it covers. */
    public var rotationLabel: String?
    /** `labels.id` for `record_label`, or `null` when the row's label was never linked to a catalog row — the state of the large majority of rows, not an edge case. Passed straight through from `flowsheet.label_id`, which carries no NOT NULL. Consumers must fall back to the free-text `record_label` on null. Same column and same contract as `FlowsheetV2TrackEntry.label_id`; the V1 and V2 field sets are separate for historical reasons, not because the column differs between them. */
    public var labelId: Int?
    public var rotationId: Int?
    public var rotationBin: RotationBin?
    public var requestFlag: Bool
    public var segue: Bool?
    public var message: String?
    public var artworkUrl: String?
    public var discogsUrl: String?
    public var releaseYear: Int?
    /** Rides the same MD-set \"not on Discogs\" flag as the Album surfaces (see `Album.discogsUnavailable`), non-nullable to match them. Deliberately camelCase — unlike its snake_case siblings in this block — to match Backend's `withDiscogsUnavailableCamelCase` serializer. The V2 union declares this field with the identical shape (`FlowsheetV2TrackEntry.discogsUnavailable`); one writer emits both surfaces, so the two declarations must not drift. */
    public var discogsUnavailable: Bool?
    /** Optional free-text reason for `discogsUnavailable`. */
    public var discogsUnavailableNote: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var spotifyUrl: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var appleMusicUrl: String?
    /** Search URL -- a query URL for the release, not a deep link to it. The two search fields are `youtube_music_url` and `soundcloud_url`; neither carries a `StreamingResolution` verdict, because a search URL can always be constructed and so has no probe/absence distinction. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations.  */
    public var youtubeMusicUrl: String?
    /** Album deep link -- a URL for this specific release. The three deep-link fields are `spotify_url`, `apple_music_url`, and `bandcamp_url`; they are the services that carry a `StreamingResolution` verdict. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations -- `bandcamp_url` is not host-checked at every seam it passes through.  */
    public var bandcampUrl: String?
    /** Search URL -- a query URL for the release, not a deep link to it. The two search fields are `youtube_music_url` and `soundcloud_url`; neither carries a `StreamingResolution` verdict, because a search URL can always be constructed and so has no probe/absence distinction. Contract-level `format: uri` only. TypeScript (`string | null`) and Swift (`String?`) treat `format: uri` as documentation-only; this repo's Python codegen pin (`pin_streaming_url_fields_to_str`, WXYC/wxyc-shared#428) holds Python to `str`, and the two downstream Python regen scripts (library-metadata-lookup, request-o-matic) must adopt the same pin -- tracked in WXYC/library-metadata-lookup#1299 and WXYC/request-o-matic#282 -- a Python consumer's `str` holds only where that pin is in place. Kotlin does not treat it as documentation-only: its generator maps `format: uri` to `java.net.URI`, so Kotlin consumers regenerating models must pin these five fields to `String` (as Python pins to `str`) or accept construction-time parse failures on rows stored before 2026-08-31. Shape enforcement lives at two runtime seams, both shipped: the Backend-Service LML-response boundary guard (`sanitizeLookupStreamingUrls`, WXYC/Backend-Service#2351) -- a host allowlist for spotify/apple_music/youtube_music/soundcloud, and well-formedness only for bandcamp (deliberate: custom-domain deep links, see the guard's own doc comment) -- and the LML read seam (`streaming_links` copy-out, `lookup/enrichment/streaming_link_validation.py`, WXYC/library-metadata-lookup#1296; read-path suppression -- the persisted row is never rewritten) -- a host check on all five, plus a well-formedness floor on youtube_music/bandcamp/soundcloud only. Those are the two seams that enforce shape, not every path these fields reach the wire through: neither is on the Backend-Service flowsheet read path, where `GET /flowsheet` host-guards only `spotify_url`/`apple_music_url` (WXYC/Backend-Service#1714) and emits the other three as stored, so a row persisted before 2026-08-31 can still carry an unchecked value. The two seams deliberately disagree on bandcamp because they see different URL populations.  */
    public var soundcloudUrl: String?
    public var artistBio: String?
    public var artistWikipediaUrl: String?
    /** Track position on the release (e.g., \"A1\", \"B2\", \"5\"). Populated when the flowsheet entry was created via the dj-site picker after release selection (catalog-track-search plan §5.3 / Track 3). String-typed to match Discogs's `release_track.position`. Null for free-text entries and legacy rows.  */
    public var trackPosition: String?
    public var metadataStatus: MetadataStatus?
    public var entryType: FlowsheetEntryType?
    /** The instant this row was logged (ISO 8601). */
    public var addTime: Date?
    /** Top-of-hour a breakpoint row marks (ISO 8601), sourced from tubafrenzy's RADIO_HOUR. Null on non-breakpoint rows and rows predating the producer rollout. Mirrors the V2 breakpoint entry. */
    public var radioHour: Date?
    /** The DJ's public on-air handle, never the DJ's legal name. Resolved through the PII-safe chain (BS#1371): per-show override -> the DJ's chosen handle (`user.djName`) -> the legacy tubafrenzy handle (`shows.legacy_dj_name`) -> `null`. */
    public var djName: String?

    public init(albumId: Int? = nil, trackTitle: String? = nil, albumTitle: String? = nil, artistName: String? = nil, recordLabel: String? = nil, rotationLabel: String? = nil, labelId: Int? = nil, rotationId: Int? = nil, rotationBin: RotationBin? = nil, requestFlag: Bool, segue: Bool? = nil, message: String? = nil, artworkUrl: String? = nil, discogsUrl: String? = nil, releaseYear: Int? = nil, discogsUnavailable: Bool? = nil, discogsUnavailableNote: String? = nil, spotifyUrl: String? = nil, appleMusicUrl: String? = nil, youtubeMusicUrl: String? = nil, bandcampUrl: String? = nil, soundcloudUrl: String? = nil, artistBio: String? = nil, artistWikipediaUrl: String? = nil, trackPosition: String? = nil, metadataStatus: MetadataStatus? = nil, entryType: FlowsheetEntryType? = nil, addTime: Date? = nil, radioHour: Date? = nil, djName: String? = nil) {
        self.albumId = albumId
        self.trackTitle = trackTitle
        self.albumTitle = albumTitle
        self.artistName = artistName
        self.recordLabel = recordLabel
        self.rotationLabel = rotationLabel
        self.labelId = labelId
        self.rotationId = rotationId
        self.rotationBin = rotationBin
        self.requestFlag = requestFlag
        self.segue = segue
        self.message = message
        self.artworkUrl = artworkUrl
        self.discogsUrl = discogsUrl
        self.releaseYear = releaseYear
        self.discogsUnavailable = discogsUnavailable
        self.discogsUnavailableNote = discogsUnavailableNote
        self.spotifyUrl = spotifyUrl
        self.appleMusicUrl = appleMusicUrl
        self.youtubeMusicUrl = youtubeMusicUrl
        self.bandcampUrl = bandcampUrl
        self.soundcloudUrl = soundcloudUrl
        self.artistBio = artistBio
        self.artistWikipediaUrl = artistWikipediaUrl
        self.trackPosition = trackPosition
        self.metadataStatus = metadataStatus
        self.entryType = entryType
        self.addTime = addTime
        self.radioHour = radioHour
        self.djName = djName
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case albumId = "album_id"
        case trackTitle = "track_title"
        case albumTitle = "album_title"
        case artistName = "artist_name"
        case recordLabel = "record_label"
        case rotationLabel = "rotation_label"
        case labelId = "label_id"
        case rotationId = "rotation_id"
        case rotationBin = "rotation_bin"
        case requestFlag = "request_flag"
        case segue
        case message
        case artworkUrl = "artwork_url"
        case discogsUrl = "discogs_url"
        case releaseYear = "release_year"
        case discogsUnavailable
        case discogsUnavailableNote
        case spotifyUrl = "spotify_url"
        case appleMusicUrl = "apple_music_url"
        case youtubeMusicUrl = "youtube_music_url"
        case bandcampUrl = "bandcamp_url"
        case soundcloudUrl = "soundcloud_url"
        case artistBio = "artist_bio"
        case artistWikipediaUrl = "artist_wikipedia_url"
        case trackPosition = "track_position"
        case metadataStatus = "metadata_status"
        case entryType = "entry_type"
        case addTime = "add_time"
        case radioHour = "radio_hour"
        case djName = "dj_name"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encodeIfPresent(albumId, forKey: .albumId)
        try container.encodeIfPresent(trackTitle, forKey: .trackTitle)
        try container.encodeIfPresent(albumTitle, forKey: .albumTitle)
        try container.encodeIfPresent(artistName, forKey: .artistName)
        try container.encodeIfPresent(recordLabel, forKey: .recordLabel)
        try container.encodeIfPresent(rotationLabel, forKey: .rotationLabel)
        try container.encodeIfPresent(labelId, forKey: .labelId)
        try container.encodeIfPresent(rotationId, forKey: .rotationId)
        try container.encodeIfPresent(rotationBin, forKey: .rotationBin)
        try container.encode(requestFlag, forKey: .requestFlag)
        try container.encodeIfPresent(segue, forKey: .segue)
        try container.encodeIfPresent(message, forKey: .message)
        try container.encodeIfPresent(artworkUrl, forKey: .artworkUrl)
        try container.encodeIfPresent(discogsUrl, forKey: .discogsUrl)
        try container.encodeIfPresent(releaseYear, forKey: .releaseYear)
        try container.encodeIfPresent(discogsUnavailable, forKey: .discogsUnavailable)
        try container.encodeIfPresent(discogsUnavailableNote, forKey: .discogsUnavailableNote)
        try container.encodeIfPresent(spotifyUrl, forKey: .spotifyUrl)
        try container.encodeIfPresent(appleMusicUrl, forKey: .appleMusicUrl)
        try container.encodeIfPresent(youtubeMusicUrl, forKey: .youtubeMusicUrl)
        try container.encodeIfPresent(bandcampUrl, forKey: .bandcampUrl)
        try container.encodeIfPresent(soundcloudUrl, forKey: .soundcloudUrl)
        try container.encodeIfPresent(artistBio, forKey: .artistBio)
        try container.encodeIfPresent(artistWikipediaUrl, forKey: .artistWikipediaUrl)
        try container.encodeIfPresent(trackPosition, forKey: .trackPosition)
        try container.encodeIfPresent(metadataStatus, forKey: .metadataStatus)
        try container.encodeIfPresent(entryType, forKey: .entryType)
        try container.encodeIfPresent(addTime, forKey: .addTime)
        try container.encodeIfPresent(radioHour, forKey: .radioHour)
        try container.encodeIfPresent(djName, forKey: .djName)
    }
}


extension FlowsheetEntryFields: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if rotationBin == .unknownDefaultOpenApi { return true }
        if metadataStatus == .unknownDefaultOpenApi { return true }
        if entryType == .unknownDefaultOpenApi { return true }
        return false
    }
}
