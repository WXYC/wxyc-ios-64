//
// Venue.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** A live-music venue whose calendar WXYC ingests (Backend-Service &#x60;venues&#x60; table). Embedded whole in &#x60;Concert&#x60; — the venue set is tiny (~21 rows), so clients get name/city in one call with no follow-up join.  */
public struct Venue: Sendable, Codable, Hashable {

    public var id: Int
    /** Stable scraper seed key (e.g. \"cats-cradle\"). */
    public var slug: String
    public var name: String
    public var city: String
    public var state: String
    public var address: String?

    public init(id: Int, slug: String, name: String, city: String, state: String, address: String?) {
        self.id = id
        self.slug = slug
        self.name = name
        self.city = city
        self.state = state
        self.address = address
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case id
        case slug
        case name
        case city
        case state
        case address
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(id, forKey: .id)
        try container.encode(slug, forKey: .slug)
        try container.encode(name, forKey: .name)
        try container.encode(city, forKey: .city)
        try container.encode(state, forKey: .state)
        try container.encode(address, forKey: .address)
    }
}

