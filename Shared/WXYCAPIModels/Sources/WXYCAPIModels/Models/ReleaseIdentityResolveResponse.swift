//
// ReleaseIdentityResolveResponse.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation

/** Response to &#x60;POST /api/v1/identity/resolve&#x60;. &#x60;identity_id&#x60; is stable across calls; &#x60;minted&#x60; distinguishes the first call (true) from subsequent lookups (false). Re-resolves do not write a new reconciliation log row — the resolver only logs the act of minting.  */
public struct ReleaseIdentityResolveResponse: Sendable, Codable, Hashable {

    /** `entity.release_identity.id` for the resolved row. Stable across calls with the same `(kind, source, external_id)`.  */
    public var identityId: Int
    public var kind: IdentityKind
    /** `true` when this call inserted a new row, `false` when an existing row was returned. Useful for callers that want to observe whether they were the first to surface a given release.  */
    public var minted: Bool

    public init(identityId: Int, kind: IdentityKind, minted: Bool) {
        self.identityId = identityId
        self.kind = kind
        self.minted = minted
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case identityId = "identity_id"
        case kind
        case minted
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(identityId, forKey: .identityId)
        try container.encode(kind, forKey: .kind)
        try container.encode(minted, forKey: .minted)
    }
}


extension ReleaseIdentityResolveResponse: UnknownCaseCheckable {
    public var containsUnknownDefaultOpenApiCase: Bool {
        if kind == .unknownDefaultOpenApi { return true }
        return false
    }
}
